base_config: ../omnire.yaml

dataset: waymo/5cams

trainer:
  optim:
    num_iters: 5000

logging:
  vis_freq: 1000
  print_freq: 100
  saveckpt_freq: 1000

data:
  pixel_source:
    downscale_when_loading: [4, 4, 4, 4, 4]
    load_smpl: true

render:
  render_full: false
  render_test: false
  render_novel: null
